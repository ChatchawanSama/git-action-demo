services:
  tuya-api:
    image: chatchawansama/git-action-demo:latest
    container_name: fusic-container-demo
    restart: unless-stopped
    networks:
      - app

networks:
  app:
    external: true
